import React from "react";
import { useParams } from "react-router-dom";

function MovieShow() {

  // const params = useParams();
  // console.log(params);

  return (
    <div>
      {/* <h3>{movies[params.key].Title}</h3> */}
      SHOW ME SOMETHING
    </div>
  );
}

export default MovieShow;